# CLAUDE.md — AI-консультант для университета

> Это файл-контекст для Claude. В новом чате с подключённой папкой проекта Claude читает его автоматически и продолжает работу без потери контекста. Человек: Isa.

---

## 1. Что строим

AI-консультант для университета, проводящий онлайн-консультацию абитуриентов/студентов. Консультация состоит из **двух частей**:

1. **Профориентационный тест RIASEC** (модель Голланда, 6 типов: Realistic, Investigative, Artistic, Social, Enterprising, Conventional). Пользователь проходит тест → получает профиль (например «SAE») → рекомендации по направлениям подготовки вуза.
2. **Онлайн-консультация на базе знаний вуза (RAG)** — ответы на вопросы о программах, факультетах, поступлении, FAQ, грунтованные на реальном контенте вуза.

Обе части — это **один граф LangGraph** с роутингом между веткой теста и веткой RAG.

## 2. Стек (зафиксирован пользователем)

- **Python** — основной язык.
- **LangChain** — загрузчики, сплиттеры, обвязка LLM/эмбеддингов.
- **LangGraph** — оркестрация диалога (граф состояний: тест → скоринг → роутинг → RAG).
- **Qdrant** — векторная БД (поднимать в Docker).
- **OpenWebUI** — основной веб-фронт (подключается к бэку как pipeline/функция).

> Важно: Qdrant, LangChain, LangGraph — это **pip-библиотеки, не коннекторы**. Ставятся в песочнице/окружении проекта, плагин для них не нужен.

## 3. Каналы (решено с пользователем)

| Канал | Как делаем | Статус инструмента |
|---|---|---|
| **OpenWebUI** | Основной фронт, pipeline к бэкенду | в стеке |
| **WhatsApp** | Через **Twilio** | плагин Twilio Developer Kit установлен |
| **Telegram** | Бот на `python-telegram-bot` | код, готового MCP нет |
| **Instagram / Facebook** | Meta Graph API + вебхуки | код |

Все каналы — тонкие адаптеры к **одному** бэкенду (один граф, одна логика).

## 4. Источник знаний для RAG (решено)

Контент берём с **сайта вуза + локальных файлов** (PDF/docx). Готового коннектора к базе знаний нет — собираем скрейпером.

## 5. Установленные плагины/инструменты

- **Engineering** — system-design, architecture (ADR), code-review, testing-strategy, documentation, debug. Использовать для проектирования и ревью.
- **Twilio Developer Kit** — канал WhatsApp (+ Verify/OTP при необходимости).
- **Bright Data** — скрейпинг сайта вуза в чистый markdown (обход JS/бот-защиты) + структурированные данные из соцсетей (навыки `scrape`, `scraper-builder`, `data-feeds`, `search`, `brightdata-sdk`/CLI `bdata`).

Чего НЕТ как плагина и не нужно — препроцессинг для RAG (очистка, чанкинг, метаданные). Это код (см. §7).

## 6. Архитектура (целевая)

```
                         ┌─────────────────────────────┐
   Каналы (адаптеры)     │  OpenWebUI / Telegram /      │
                         │  WhatsApp(Twilio) / Meta     │
                         └──────────────┬──────────────┘
                                        │ единый API (FastAPI)
                                        ▼
                         ┌─────────────────────────────┐
                         │     LangGraph граф           │
                         │  ┌──────────┐  ┌──────────┐  │
                         │  │ RIASEC   │→ │ Роутер   │  │
                         │  │ ветка    │  │ намерения│  │
                         │  └──────────┘  └────┬─────┘  │
                         │                     ▼        │
                         │              ┌──────────┐    │
                         │              │ RAG      │    │
                         │              │ консульт.│    │
                         │              └────┬─────┘    │
                         └───────────────────┼──────────┘
                                             ▼
                                   ┌──────────────────┐
                                   │  Qdrant (Docker) │
                                   └──────────────────┘
```

## 7. Ingestion-пайплайн для RAG (всё — код)

`scrape → clean → chunk → metadata → upsert`

1. **scrape** — Bright Data (`bdata scrape` / SDK): страницы сайта вуза → markdown; локальные PDF/docx → `unstructured`.
2. **clean** — `trafilatura`/`unstructured` (основной текст), `ftfy`+regex (битые символы), дедуп по хешам.
3. **chunk** — `langchain-text-splitters`, рекурсивный по markdown-заголовкам с overlap; для длинных страниц — семантический чанкинг.
4. **metadata** — на каждый чанк: `source_url`, `title`, `faculty`, `program`, `doc_type`, `lang`, `updated_at`.
5. **upsert** — `qdrant-client`: коллекция, эмбеддер, батчевый upsert с payload + индексы для фильтров.

## 8. Предлагаемая структура репозитория

```
university-ai-consultant/
├── CLAUDE.md                  # этот файл
├── docker-compose.yml         # qdrant + (opt) openwebui
├── pyproject.toml / requirements.txt
├── .env.example               # ключи: OpenAI/эмбеддер, Qdrant, Twilio, Telegram, Meta
├── ingestion/                 # scrape→clean→chunk→metadata→upsert
│   ├── scrape.py
│   ├── clean.py
│   ├── chunk.py
│   └── load_qdrant.py
├── riasec/                    # тест: вопросы, скоринг, маппинг на программы
│   ├── questions.yaml
│   ├── scoring.py
│   └── recommend.py
├── graph/                     # LangGraph
│   ├── state.py
│   ├── nodes.py
│   └── build.py
├── api/                       # FastAPI бэкенд (единая точка для каналов)
│   └── main.py
├── channels/                  # адаптеры
│   ├── telegram_bot.py
│   ├── whatsapp_twilio.py
│   └── meta_webhook.py
├── docs/adr/                  # архитектурные решения (Engineering: architecture)
└── tests/
```

## 8.1 Сделано (2026-06-10): RIASEC-тест ✅

- `app/riasec.py` — адаптация **O*NET Interest Profiler Short Form**: 60 пунктов
  (по 10 на тип), Ликерт 1–5, языки ru/ky/en, скоринг + код Голланда + маппинг
  на программы бакалавриата Ала-Тоо (топ-6 по конгруэнтности, веса 3/2/1).
- `app/static/test.html` — страница теста `/test` в стиле чата: согласие на
  обработку данных, переключатель ru/ky/en, прогресс, экран результатов
  (код, бары по 6 типам, программы), кнопка «Обсудить с ИИ-консультантом».
- API: `GET /riasec/api/questions`, `POST /riasec/api/submit`, `GET /riasec/api/result`,
  `GET /admin/api/riasec`. Результаты — в SQLite (`riasec_results`) + ход теста
  логируется в `messages` (source=`riasec-test`) → видно в `/admin/logs`.
- Интеграция: агент предлагает тест ссылкой `/test` (см. SYSTEM_PROMPT в
  `app/rag.py`); ссылка из чата несёт `?chat=<id>`; после теста результат
  привязан к сессии чата, бэкенд подмешивает профиль в контекст LLM
  (заголовок `X-Riasec-Id` или lookup по `session_id`); возврат в чат рисует
  карточку результатов и автозапускает обсуждение.
- Тесты: `tests/test_riasec.py` (pytest, 10 шт., LLM/Qdrant не нужны).
- Решение и обоснование: `docs/adr/0002-riasec-test.md`.
- TODO: проверка кыргызских формулировок носителем; валидация маппинга программ.

## 8.2 Сделано (2026-06-16): справочник кабинетов ✅

- В `data/` лежал повреждённый `Кабинеты.csv` (вся кириллица → `?`, ingestion
  его пропускал по фильтру `raw_corruption_ratio`). Источник был на рабочем
  столе в `alatoo info/` (`Кабинеты(UTF-8).csv` — целый UTF-8, и `Кабинеты.ods`).
- Преобразовано в `data/Кабинеты.md` (UTF-8, 337 сотрудников, 119 кабинетов,
  44 подразделения). Две проекции для надёжного retrieval:
  **«По подразделениям»** (заголовок = отдел/кафедра, под ним люди с должностью
  и кабинетом) и **«Указатель по кабинетам»** (обратный индекс кабинет → кто
  сидит). Кириллические/латинские буквы корпусов (А/A, С/C…) нормализованы при
  группировке, отображается оригинал. Значения-мусор `?` → «кабинет не указан».
- Повреждённый `Кабинеты.csv` удалён, чтобы не дублировать чанки.
- Чтобы ИИ начал отвечать по кабинетам — переиндексировать:
  `docker compose run --rm backend python -m ingestion.ingest` (без `--recreate`
  добавит/обновит; чанки дедуплицируются по хешу). Нужны поднятый Qdrant и
  эмбеддер (BGE-M3).
- TODO: при желании добавить телефоны/почты, если появятся в источнике.

## 9. С чего начать в новом чате (порядок работ)

1. **Подтвердить открытые вопросы** (§10) с Isa.
2. **Поднять Qdrant** в Docker (`docker-compose.yml`) + создать окружение и поставить зависимости.
3. **RIASEC-тест** (можно параллельно): составить 42–48 вопросов под вуз, скоринг по 6 типам, маппинг профиль→направления подготовки. Сохранить в `riasec/`.
4. **Первый проход ingestion**: Bright Data scrape одной-двух страниц сайта вуза → clean → chunk → metadata → upsert в Qdrant. Проверить качество чанков.
5. **Скелет LangGraph**: state, узлы (тест / роутер / RAG), сборка графа.
6. **FastAPI бэкенд** поверх графа.
7. **Каналы**: сначала OpenWebUI, затем Telegram, WhatsApp, Meta.
8. По ходу фиксировать решения как ADR (`docs/adr/`) и писать тесты.

Рекомендуемый старт: пункты **2–4 параллельно**.

## 10. Открытые вопросы (уточнить у Isa перед стартом)

- URL сайта вуза и примерные разделы для скрейпинга; есть ли локальные PDF/docx и где.
- Какой LLM и эмбеддер использовать (OpenAI / локальная модель через OpenWebUI / другое) и язык(и) контента (рус/кыр/англ?).
- Готовая методичка/нормы RIASEC под вуз или составляем вопросы с нуля.
- Есть ли уже аккаунты/ключи: Twilio (WhatsApp), Telegram Bot token, Meta App, Bright Data API key.
- Где деплой (сервер вуза / облако / Docker на VPS).

## 11. Как Isa создаёт новый проект

1. Создать на компьютере папку, например `university-ai-consultant`.
2. Положить в неё **этот файл** (`CLAUDE.md`).
3. В приложении начать **новый чат** и подключить (mount) эту папку как рабочую.
4. Написать: «Продолжаем проект, читай CLAUDE.md» — я прочитаю контекст и начну с §9.

> Плагины (Engineering, Twilio, Bright Data) уже установлены глобально и будут доступны в новом чате.
